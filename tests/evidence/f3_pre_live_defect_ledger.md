# LEDGER COMPLETO DE DEFECTOS LOCALES F3 — PRE-LIVE CLOSURE

**Base:** main @ 86fad8b  
**Alcance:** 6 Workflows F3, contratos de dataflow, binarios, retry policies y persistencia relacional.

---

| Defect ID | Workflow | Node | Severity | Baseline Requirement | Actual Behavior | Expected Behavior | Root Cause | Planned Fix | Files Affected | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **PRELIVE-001** | WF-ING-003 | `SHA-256 Binary` | P1 | Binary data must be preserved through SHA computation | Crypto v1 strips binary property from item output | Binary data preserved intact alongside SHA-256 hex | Crypto node v1 marks binaryProcessed and drops binary | Replace Crypto node with Code node using `getBinaryDataBuffer` | `WF-ING-003_PROCESS_MEDIA.json` | **FIXED** |
| **PRELIVE-002** | WF-ING-003 | `Telegram Get File` | P1 | Canonical context must survive Telegram download | Telegram node 1.2 replaces item JSON with Telegram file metadata | Canonical envelope context restored with Telegram metadata | Telegram node output lacks input JSON envelope | Add `Restore Telegram Media Context` Code node | `WF-ING-003_PROCESS_MEDIA.json` | **FIXED** |
| **PRELIVE-003** | WF-ING-003 | `Archive Original in Drive` | P1 | Drive originals must not be re-uploaded to Drive | All media unconditionally routed to Drive upload | Telegram media archived to Drive; Drive media skips upload | Missing source-based branching prior to Drive archive | Add source branching in WF-ING-003 | `WF-ING-003_PROCESS_MEDIA.json` | **FIXED** |
| **PRELIVE-004** | WF-ING-003 | `Upsert Asset + Location RPC` | P1 | Telegram file archived in Drive must record 2 locations | Single location recorded | 1 asset with both Telegram and Drive locations recorded | Workflow only invoked upsert RPC once | Invoke upsert RPC for Telegram location then Drive location | `WF-ING-003_PROCESS_MEDIA.json` | **FIXED** |
| **PRELIVE-005** | WF-ING-004 | `Check Ingestion Duplicate` | P1 | Duplicate Drive events must terminate without side effects | Duplicate events flowed into Drive download | Duplicate events terminate immediately with 0 downloads | Missing branch switch after duplicate check | Add duplicate switch routing duplicate to terminal no-op | `WF-ING-004_DRIVE_WATCH.json` | **FIXED** |
| **PRELIVE-006** | WF-ING-005 | `Register Reconciled Ingestion` | P1 | Reconciliation must preserve candidate context per item | Postgres executeQuery drops input payload context | Candidate context restored per item index | Postgres node outputs only `{ res: ... }` | Add `Restore Reconciled Candidate Context` node | `WF-ING-005_DRIVE_RECONCILIATION.json` | **FIXED** |
| **PRELIVE-007** | WF-ING-005 | Multiple | P1 | Multi-item reconciliation must not cross-contaminate | `.first()` used across multi-item array | Each candidate preserves its own data | Blind use of `.first()` in multi-item workflow | Replace `.first()` with index-based / matching pairing | `WF-ING-005_DRIVE_RECONCILIATION.json` | **FIXED** |
| **PRELIVE-008** | All F3 | Multiple | P2 | No invented fallbacks (file_size, language, timezone) | Fallback defaults (`|| 'telegram'`, `|| 0`) present | Real metadata preserved, unknown preserved as NULL/unknown | Defensive fallbacks masking missing upstream data | Remove invented defaults and validate strictly | All 6 F3 Workflows | **FIXED** |
| **PRELIVE-009** | WF-ING-003 | `Media Type Router` | P2 | Unsupported safe media must not hang in processing | Unsupported media routed nowhere (hangs in processing) | Explicit terminal error status persisted ('UNSUPPORTED_MEDIA_TYPE') | Media router omitted 'unsupported' branch handler | Add terminal handler for unsupported media | `WF-ING-003_PROCESS_MEDIA.json` | **FIXED** |
| **PRELIVE-010** | WF-AI-001 | `Gemini Transcribe Interaction` | P1 | Language hint must not invent 'es-AR' | Hardcoded `language_codes: ["es-AR"]` sent | Send `language_codes: []` for autodetection | Hardcoded locale assumption | Send empty array for autodetection, persist detected language or NULL | `WF-AI-001_TRANSCRIBE.json` | **FIXED** |
| **PRELIVE-011** | WF-AI-001 | `Extract Upload Session URL` | P1 | Gemini File resource state must be validated | Assumed file active immediately | Validate state (FAILED -> error, PROCESSING -> wait/error) | Missing file state check | Add file state validation in normalize node | `WF-AI-001_TRANSCRIBE.json` | **FIXED** |
| **PRELIVE-012** | WF-AI-003 | `OpenAI Luna / Gemini Vision` | P1 | Vision nodes must access buffer via helper and validate MIME | `$binary.data.data` read directly with `image/jpeg` fallback | `getBinaryDataBuffer` used, MIME strictly validated | Direct binary property access | Add `Validate and Prepare Visual Binary` Code node | `WF-AI-003_ANALYZE_VISUAL.json` | **FIXED** |
| **PRELIVE-013** | WF-AI-003 | `Persist Visual Source Text` | P2 | Visual description language must be nullable | Hardcoded 'es-AR' passed to RPC | Language passed as NULL (visual description) | Hardcoded locale parameter | Pass NULL as language for visual description | `WF-AI-003_ANALYZE_VISUAL.json` | **FIXED** |
| **PRELIVE-014** | WF-ING-006 | `Native Extract From File` | P1 | Document extract must use appropriate native operation | `operation: 'text'` used for all documents | Native operations routed by format (pdf, text, csv, etc.) | Generic text operation used | Add `Document Format Router` and native operations | `WF-ING-006_DOCUMENT_EXTRACT.json` | **FIXED** |
| **PRELIVE-015** | WF-ING-003 | `Persist Completed Media Status` | P1 | Ingestion completed only after verified child persistence | Parent marked completed unconditionally on subworkflow return | Parent verifies child `source_text_id` before marking completed | Missing verification gate before completion update | Add `Verify Child Persistence Before Completion` gate | `WF-ING-003_PROCESS_MEDIA.json` | **FIXED** |
| **PRELIVE-016** | All F3 | External create/paid nodes | P1 | Non-idempotent create and paid compute must not blind retry | `retryOnFail: true` enabled on create/paid operations | `retryOnFail: false` on Drive upload, Gemini finalize & interactions | Default retryOnFail on external create nodes | Set `retryOnFail: false` on non-idempotent operations | All 6 F3 Workflows | **FIXED** |
